# 📁 项目文件结构说明

```
YoYoFileManage/
│
├── 📊 数据集相关
│   └── datasets/                    # YOLO格式数据集
│       ├── data.yaml               # 数据集配置文件 ⭐
│       ├── train/                  # 训练集
│       │   ├── images/            # 训练图片
│       │   └── labels/            # 训练标注（.txt）
│       ├── valid/                  # 验证集
│       │   ├── images/            # 验证图片
│       │   └── labels/            # 验证标注（.txt）
│       └── test/                   # 测试集
│           ├── images/            # 测试图片
│           └── labels/            # 测试标注（.txt）
│
├── 🚀 训练相关
│   ├── train_yolo.py              # 主训练脚本 ⭐ (最重要)
│   ├── train_config.py            # 训练配置文件
│   └── quick_start.py             # 快速开始示例（5分钟测试）
│
├── 🔍 测试相关
│   ├── test_yolo.py               # 模型测试脚本 ⭐
│   └── monitor.py                 # 训练监控工具
│
├── 🏷️ 标注审核相关
│   ├── yolo2label_studio.py       # YOLO转Label Studio脚本 ⭐
│   ├── convert_to_labelstudio.sh  # 批量转换脚本（可执行）
│   └── label_studio_config.xml    # Label Studio配置模板
│
├── 📦 环境配置
│   ├── requirements.txt           # Python依赖列表 ⭐
│   └── setup.sh                   # 自动安装脚本（可执行）
│
├── 📖 文档
│   ├── README.md                  # 项目说明 ⭐
│   ├── docs/                      # 详细文档目录
│   │   ├── GUIDE.md              # 完整使用指南 ⭐ (新手必读)
│   │   ├── PROJECT_STRUCTURE.md  # 本文件
│   │   ├── YOLO_TO_LABELSTUDIO.md # 标注转换指南 ⭐
│   │   ├── LABELSTUDIO_QUICKSTART.md # Label Studio快速开始
│   │   └── ...                   # 其他文档
│
├── 📁 训练输出（运行后自动生成）
│   └── runs/
│       ├── detect/                # 训练结果
│       │   └── yolo12n_person_head/
│       │       ├── weights/
│       │       │   ├── best.pt    # 最佳模型 ⭐ (用于部署)
│       │       │   └── last.pt    # 最后权重
│       │       ├── results.png    # 训练曲线 ⭐
│       │       ├── results.csv    # 详细数据
│       │       └── ...            # 其他可视化图表
│       └── predict/               # 预测结果
│           └── test/              # 测试结果图片
│
└── 🗂️ 其他
    ├── main.py                    # 原始示例文件
    └── venv/                      # Python虚拟环境（运行setup.sh后生成）
```

---

## 📌 文件用途速查

### ⭐ 核心文件（必须了解）

| 文件 | 用途 | 何时使用 |
|------|------|---------|
| `train_yolo.py` | 训练模型 | 开始训练时 |
| `test_yolo.py` | 测试模型 | 训练完成后 |
| `requirements.txt` | 依赖列表 | 安装环境时 |
| `GUIDE.md` | 使用指南 | 遇到问题时 |
| `datasets/data.yaml` | 数据集配置 | 检查数据集 |

### 📚 辅助文件

| 文件 | 用途 | 何时使用 |
|------|------|---------|
| `setup.sh` | 自动安装 | 初次配置环境 |
| `monitor.py` | 监控训练 | 训练过程中 |
| `train_config.py` | 配置示例 | 自定义参数 |
| `quick_start.py` | 快速测试 | 5分钟体验 |
| `README.md` | 项目概览 | 了解项目 |

---

## 🎯 典型使用流程

### 第一次使用

1. **阅读文档**
   ```bash
   cat GUIDE.md    # 完整使用指南
   ```

2. **安装环境**
   ```bash
   ./setup.sh      # 自动安装
   # 或
   pip install -r requirements.txt  # 手动安装
   ```

3. **快速测试**（可选）
   ```bash
   python quick_start.py   # 5分钟测试流程
   ```

4. **正式训练**
   ```bash
   python train_yolo.py    # 开始训练
   ```

5. **监控训练**（新终端窗口）
   ```bash
   python monitor.py watch  # 实时监控
   ```

6. **测试模型**
   ```bash
   python test_yolo.py     # 评估性能
   ```

---

## 📂 输出文件说明

### 训练输出 (`runs/detect/yolo12n_person_head/`)

```
yolo12n_person_head/
├── weights/
│   ├── best.pt              ⭐ 验证集上最佳模型（部署用）
│   └── last.pt              最后一个epoch（继续训练用）
│
├── 📊 可视化图表
│   ├── results.png          训练曲线（loss、mAP等）
│   ├── confusion_matrix.png 混淆矩阵
│   ├── PR_curve.png         精度-召回率曲线
│   ├── F1_curve.png         F1分数曲线
│   ├── P_curve.png          精度曲线
│   └── R_curve.png          召回率曲线
│
├── 📄 数据文件
│   ├── results.csv          详细训练数据（每个epoch）
│   └── args.yaml            训练参数备份
│
└── 🖼️ 样例图片
    ├── train_batch0.jpg     训练批次示例
    ├── train_batch1.jpg
    ├── val_batch0_pred.jpg  验证集预测
    └── val_batch0_labels.jpg 验证集标签
```

### 预测输出 (`runs/predict/test/`)

```
test/
├── image1.jpg               预测结果图片（带标注框）
├── image2.jpg
├── ...
└── labels/                  预测标注（可选）
    ├── image1.txt
    └── image2.txt
```

---

## 🔍 文件查找指南

### 想要...

**查看训练效果如何？**
→ `runs/detect/yolo12n_person_head/results.png`

**获取最佳模型？**
→ `runs/detect/yolo12n_person_head/weights/best.pt`

**查看预测结果？**
→ `runs/predict/test/` 目录下的图片

**查看详细训练数据？**
→ `runs/detect/yolo12n_person_head/results.csv`

**修改训练参数？**
→ 编辑 `train_yolo.py` 的配置部分

**遇到问题？**
→ 先看 `GUIDE.md` 的"常见问题"部分

**了解数据集配置？**
→ 查看 `datasets/data.yaml`

---

## 📋 文件大小参考

| 文件/目录 | 大小 |
|----------|------|
| `yolo12n.pt` (预训练) | ~6 MB |
| `best.pt` (训练后) | ~6 MB |
| `datasets/` (数据集) | 取决于数据量 |
| `runs/` (训练输出) | 10-50 MB |
| `venv/` (虚拟环境) | ~500 MB |

---

## ⚠️ 重要提示

### ✅ 可以删除

- `runs/` - 训练结果（如果不需要了）
- `venv/` - 虚拟环境（可重新创建）
- `main.py` - 示例文件（不影响训练）

### ❌ 不要删除

- `datasets/` - 数据集
- `train_yolo.py` - 训练脚本
- `requirements.txt` - 依赖列表
- `datasets/data.yaml` - 数据集配置

### 💾 建议备份

- `runs/detect/*/weights/best.pt` - 训练好的模型
- `runs/detect/*/results.csv` - 训练数据
- `datasets/` - 数据集（如果没有其他备份）

---

## 🎓 学习路径建议

### 初学者
1. 阅读 `GUIDE.md`
2. 运行 `quick_start.py` 快速体验
3. 运行 `train_yolo.py` 正式训练
4. 查看 `results.png` 了解训练效果

### 进阶用户
1. 修改 `train_yolo.py` 的参数
2. 使用 `monitor.py` 监控训练
3. 使用 `test_yolo.py` 评估模型
4. 导出模型到其他格式

### 高级用户
1. 自定义数据增强策略
2. 调整学习率和优化器
3. 尝试不同模型规模
4. 集成到实际应用

---

**有问题？查看 `GUIDE.md` 获取详细帮助！** 📖
